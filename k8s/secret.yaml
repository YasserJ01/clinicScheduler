apiVersion: v1
kind: Secret
metadata:
  name: clinic-scheduler-secrets
  namespace: clinic-scheduler
  annotations:
    # DEPRECATED: This static Secret is a development-only fallback.
    # In production, replace with External Secrets Operator:
    #   kubectl apply -f k8s/cluster-secret-store.yaml
    #   kubectl apply -f k8s/external-secret.yaml
    # Vault path: clinic-scheduler/production
    # Then remove this file.
    app.kubernetes.io/managed-by: "manual-fallback"
type: Opaque
data:
  # Base64 encoded development defaults — NOT for production use.
  # Generate with: echo -n "your-value" | base64
  SECRET_KEY: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
  DB_PASSWORD: Y2xpbmljcGFzcw==
  REDIS_PASSWORD: cmVkaXNwYXNz
